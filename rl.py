# rl.py
import random

def compute_action(state):
    """
    Dummy function to compute an action based on the game state.
    Replace with actual RL model inference.
    """
    actions = ['up', 'down', 'left', 'right']
    return random.choice(actions)
